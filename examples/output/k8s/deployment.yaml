# Generated by BoneScript compiler.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-platform
  labels:
    app: inventory-platform
    version: "1.0.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: inventory-platform
  template:
    metadata:
      labels:
        app: inventory-platform
    spec:
      containers:
        - name: inventory-platform
          image: inventory-platform:1.0.0
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3000"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: inventory-platform-secrets
                  key: database-url
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: inventory-platform-secrets
                  key: jwt-secret
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: inventory-platform-secrets
                  key: redis-url
                  optional: true
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-platform
spec:
  selector:
    app: inventory-platform
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP
